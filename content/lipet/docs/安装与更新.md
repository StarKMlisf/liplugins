# 安装与更新

## 0.28.0 · 骑乘、命名与多行名牌

原版骑乘新增自动迈步和 LOOK/MOVEMENT 朝向；ModelEngine 保留独立驾驶控制。
新增 naming.yml 命名规则与 nameplate.lines 多行排版，兼容已有配置和名字。
本版为扩展开发第一批，品质/性格/闪光、材料进化、探索派遣尚未上线。
配置说明见本次更新文档。

## 0.27.29 · 跟随与模型骑乘稳定性

地面宠物在主人附近停靠，减少贴身重叠；脱战返回不再反复重置路径。
模型虚拟座位跨 Folia 区域时保留骑乘判断，暂停不安全的读取；减少骑乘时名牌重复刷新。
不新增必填配置，具体行为及验证范围见本次更新说明。

## 0.27.28 · 单宠名牌与战斗跟随

单宠物 nameplate 支持覆盖及热重载；主人走远时宠物脱战返回，返回途中不重新接战。
补充模型驾驶座 Bukkit 输入同步与骑乘期间技能控制。两份龙配置的缩进问题与模型测试边界见本次更新说明。

## 0.27.27 更新

正常停服替换 Jar 后完整启动。新配置 `defaults.behavior.riding-unlock-level` 默认 1，已有宠物不受限；可按单宠物覆盖。[MM 模型与骑乘配置](更新日志-2026-09-12-MM模型与骑乘等级.md)。

## 0.27.26 独立背包贴图标题

维护窗口备份后替换 `LiPet-0.27.26-SNAPSHOT.jar` 并完整启动。默认关闭皮肤，普通背包行为保留；匹配资源包安装后再配置 `gui.yml` 中 `inventory-skin`。容量、物品、数据库、商品价格与权限不由此配置修改。详见 [贴图标题说明](../changelog-2026-09-08-inventory-skin/)。

## 0.27.25 生命周期排查

维护窗口备份后替换 `LiPet-0.27.25-SNAPSHOT.jar`，完整重启 Minecraft 服务器。本版修复重载意外回血和死亡保存失败后的收回路径，不新增配置；待保存死亡事件尚非磁盘台账。不要通过 PlugManX 热卸载来验收本次修复，详见 [逐项排查报告](../audit-2026-09-06-lifecycle/)。

## 0.27.24 死亡扣级惩罚

维护窗口备份后安装 `LiPet-0.27.24-SNAPSHOT.jar` 并完整启动。旧服缺失配置会补入默认启用的死亡扣 1 级、最低 1 级规则；不需要时设置 `death-penalty.enabled: false`。实际掉级还会清空当前级经验并扣回对应属性点。配置热重载、死亡、重启和复活均经 Folia 隔离服验证，详见 [完整规则](../changelog-2026-09-06-death-penalty/)。

> 本页从 LiPet `0.27.24-SNAPSHOT` 完整 Wiki 同步整理；需要连续查阅全部功能时，请返回 [LiPet 完整 Wiki](../home/)。

## 0.27.23 Folia 专项排查

维护窗口备份后仅保留 `LiPet-0.27.23-SNAPSHOT.jar` 并完整启动。无新增配置，不覆盖管理员设置。修复调度退休、跨区战斗/捕捉和 GUI 点击时序；离线捕捉退款改在捕捉位置掉落，请回原地拾取。已完成 366 项测试与 Folia 26.2 定向验证，仍有并发背包和重载等风险，详见 [排查报告](../folia-audit-2026-09-06/)。

## 0.27.22 GUI 物品保护修复

停服备份后只保留 `LiPet-0.27.22-SNAPSHOT.jar` 并完整启动。全部只读 LiPet 菜单现在同时拦截点击和触及顶部槽位的拖拽；玩家自身背包和宠物独立背包的正常整理、存取不受影响。本版无新增配置，也不覆盖 `gui.yml`。详见 [本次更新说明](../changelog-2026-09-06-gui-item-protection/)。

## 0.27.21 Folia 认主与召唤时序修复

停服备份后只保留 `LiPet-0.27.21-SNAPSHOT.jar` 并完整启动。Folia 新实体的最终召唤确认改到宠物自己的下一次 Entity Scheduler 任务，不再因生成当 tick 暂时 `isValid=false` 而误回滚。无新增配置，0.27.20 的喂食冷却和已有管理员设置保持不变。Folia 26.2 已验证带外观狼召唤、跨 1024 格区域认主与收回。详见 [本次更新说明](../changelog-2026-09-06-folia-owner-spawn/)。

## 0.27.20 重进认主与喂食冷却

停服备份后只保留 `LiPet-0.27.20-SNAPSHOT.jar` 并完整启动。旧配置会补全 `feeding.cooldown-seconds: 1.0` 和冷却提示，不覆盖管理员现有配置；修改冷却后可执行 `/lipet reload`。默认兼容 API 与 Paper 26.2 API 各 359 项测试通过，真实狼实体完成重进主人重绑与连续喂食冷却验证。详见 [本次更新说明](../changelog-2026-09-06-relogin-feeding-cooldown/)。
## 0.27.19 属性加点与属性归属修复

停止并备份服务器后，只保留 `LiPet-0.27.19-SNAPSHOT.jar` 再完整启动。旧配置会补全 `pet-attribute-no-points` 与 `pet-attribute-invalid-action`，不会覆盖管理员自定义文本、GUI 或宠物属性。两套 API 清理构建各 353 项测试通过，Paper 26.2 Build 111 完成启动、状态、重载和安全停止。详见 [本次更新说明](../changelog-2026-09-05-attribute-allocation/)。

## 0.27.18 Residence 天气与属性加点修复

停服备份后只保留 `LiPet-0.27.18-SNAPSHOT.jar` 并完整启动；`/lipet reload` 不能替换本次代码。无需新增配置，也不会覆盖 Residence Flag、宠物属性、GUI 或其他管理员设置。

安装 Residence 时，控制台应出现“已挂钩 Residence，领地局部晴天会保护 LiPet 烈焰人”日志。世界处于雨天时，`sun=true` 且 `rain` 未启用的领地会保护 LiPet 烈焰人的淋雨伤害；普通烈焰人和真实水/气泡柱伤害保持原版。连续属性加点保存成功后会直接显示本次权威档案。两套 API 清理构建各 349 项测试通过，Paper 26.2 Build 111 五个天气/水边界通过。详见 [本次更新说明](../changelog-2026-09-05-residence-weather-attributes/)。

## 0.27.17 风弹喂食冲突修复

备份后正常停止 Minecraft 服务端，替换为 `LiPet-0.27.17-SNAPSHOT.jar`，只保留一份 LiPet Jar 并完整启动。无需修改配置，继续用风弹喂旋风人。`/lipet reload` 不会替换旧代码。

两套 API 全量构建各 343 项测试通过；Paper 26.2 包处理器对照验证喂食只扣一颗且不发射、副手不扣除、非主人失败不浪费、普通投掷与创造模式保持正常。详见 [本次更新说明](更新日志-2026-09-05-风弹喂食冲突修复.md)。

## 0.27.16 捕捉幼体认主修复

停服备份后只保留 `LiPet-0.27.16-SNAPSHOT.jar` 并完整启动。旧版已经长大的捕捉幼体会在重新召唤或区块恢复时按数据库中的 `adult=false` 恢复幼体并锁龄；无需删除宠物或数据库。本版不新增配置，不覆盖食物、模型、速度、商城或其他管理员设置。

默认构建与 Paper 26.2 Profile 各 333 项测试通过。Paper 26.2 Build 111 真实狼、猫、马完成成年边界保留和 LiPet 幼体锁龄验证，测试服安全关闭。详情见 [0.27.16 捕捉幼体认主修复](更新日志-2026-09-04-捕捉幼体认主修复.md)。

## 0.27.15 快乐恶魂移动修复

停服备份后只保留 `LiPet-0.27.15-SNAPSHOT.jar` 并完整启动。该问题位于 Jar 内的跟随、战斗和骑乘控制代码，旧版执行 `/lipet reload` 无法修复。已有速度、跟随距离、传送距离、骑乘类型、模型和食物配置均保留。

默认构建与 Paper 26.2 Profile 各 330 项测试通过。Paper 26.2 Build 111 的真实成年快乐恶魂完成跟随抬升、骑乘原地转向、持续前进转向和多层座位验证，测试服安全关闭。详情见 [0.27.15 快乐恶魂移动修复](更新日志-2026-09-04-快乐恶魂移动修复.md)。

## 0.27.14 原版宠物专属食物校准

停服备份后只保留 `LiPet-0.27.14-SNAPSHOT.jar` 并完整启动。新安装会生成修正后的专属食物；旧服已有 `growth.foods` 不覆盖，需要按 [0.27.14 更新日志](更新日志-2026-09-04-原版宠物专属食物校准.md) 手动替换对应节点，然后执行 `/lipet reload`。

默认兼容构建与 Paper 26.2 Profile 各 327 项测试通过。Paper 26.2 Build 111 实际生成 87 个原版宠物文件和 2 个自定义示例，实读 8 组修正文件后安全关闭。

## 0.27.13 默认喂食经验平衡

停服备份后只保留 `LiPet-0.27.13-SNAPSHOT.jar` 并启动。新安装、新生成的原版宠物和完全缺少食物节点的官方文件会使用每次 `4` 点喂食经验。旧服已有 `growth.foods` 不覆盖；要采用新值，请把相应食物的 `experience` 手动改为 `4`，再执行 `/lipet reload`。

默认与 Paper 26.2 Profile 各 325 项测试通过；Paper 26.2 Build 111 全新生成 89 个宠物文件，公共食物和烈焰人专属食物均验证为 `4`。详情见 [0.27.13 默认喂食经验平衡](更新日志-2026-09-04-默认喂食经验平衡.md)。

## 0.27.12 Folia 名牌跨区修复

停服备份后移走旧 `0.27.9` 等 LiPet Jar，只保留 `LiPet-0.27.12-SNAPSHOT.jar` 并完整启动。`/lipet reload` 不能替换 Jar 内的实体调度代码。本版没有新增配置，不覆盖 `nameplate.hide-while-ridden`、文字、偏移或背景。

新版在名牌刷新前检查宠物区域所有权，迁区临界点由实体调度器下一 tick 重试；最终失败保留当前显示状态，不再在异常分支重复读取乘客。完整说明与验收步骤见 [0.27.12 Folia 名牌跨区修复](更新日志-2026-09-04-Folia名牌跨区.md)。

## 0.27.11 原版食物与成长持久化修复

停服备份后替换为 `LiPet-0.27.11-SNAPSHOT.jar` 并完整启动。插件会为完全缺少 `growth.foods` 的官方原版宠物文件补入物种食物，例如烈焰人使用烈焰粉；已有食物节点、数值、注释和自定义宠物文件不覆盖。控制台会显示实际补全文件数。

战斗经验改为按宠物合并积压写入，并保持玩家操作顺序；数据库 revision 冲突重读重试，回收与关服保存等待经验批次完成。默认构建与 Paper 26.2 Profile 各 321 项测试通过，同一最终 Jar 已在 Paper 26.1.2 Build 70 与 Paper 26.2 Build 111 完成 SQLite 启动、状态检查、重载和安全关闭。详情见 [0.27.11 更新日志](更新日志-2026-09-04-原版食物与成长持久化.md)。

## 0.27.10 技能书与敏捷速度修复

停服备份后替换为 `LiPet-0.27.10-SNAPSHOT.jar` 并完整重启，不能只对旧 Jar 执行 `/lipet reload`。本版没有新增必须配置的节点，也不会覆盖已有技能、速度、GUI 文本或管理员注释。技能书改为持久预扣并按玩家串行升级，失败、满级、掉线或重启后退款；敏捷、基础速度和速度技能统一作用于实体、跟随、战斗、GUI 与 PAPI。

默认兼容构建与 Paper 26.2 Profile 各 315 项测试通过；同一成品在 Paper 26.1.2 Build 70 与 Paper 26.2 Build 111 的真实 SQLite 中连续完成 5 次升级，第 6 次稳定返回满级。两服真实狼实体速度均从 `0.35` 在加 1 点敏捷后变为 `0.355`，并安全关闭。详情见 [0.27.10 技能书与敏捷速度](更新日志-2026-09-03-技能书与敏捷速度.md)。

## 0.27.9 商城、飞行加速与回收修复

停服备份后替换为 `LiPet-0.27.9-SNAPSHOT.jar` 并重启，执行 `/lipet status` 核对实际加载版本。本版将道具商城余额不足提示与宠物商城分开，物品发放任务退休或失败时会结束交易并退款；宠物速度会同步实体支持的 `FLYING_SPEED`，骑乘按住疾跑键时使用可配置倍率；命令、管理 GUI 和信号棒共用可靠的回收反馈。

旧配置只补 `item-shop-insufficient-funds`、`pet-store-pending`、`pet-store-feedback-failed` 和 `behavior.riding-sprint-multiplier` 等缺失节点与中文注释，不覆盖管理员已有价格、货币 ID、骑乘速度或消息。默认兼容构建与 Paper 26.2 Profile 各 312 项测试通过；同一成品在 Paper 26.1.2 Build 70、26.2 Build 111 各完成 8 轮真实 SQLite 回收，并验证鹦鹉飞行属性与 1.5 倍疾跑。详情见 [0.27.9 商城、飞行加速与回收](更新日志-2026-09-02-商城飞行加速与回收.md)。

## 0.27.8 召唤无响应修复

停服备份后替换为 `LiPet-0.27.8-SNAPSHOT.jar` 并重启，执行 `/lipet status` 核对实际加载版本。旧版实体任务已受理却在执行前退休时，可能留下未完成的召唤 Future，使同一玩家的后续请求一直排队；`/lipet reload` 不重建玩家操作队列，所以不能代替这次换 Jar 和重启。

新版补全实体任务完成与召唤回滚分支，同步依赖错误和查询失败不再留下永久占位。命令与仓库等待超过 100 tick 时仅提示一次，`messages.yml -> pet-call-pending` 设为 `""` 可关闭；该提示不取消操作、不解锁正在进行的数据写入。

保留宠物数据库、管理员配置与 0.27.7 的骑乘修复，不需要清库或删除宠物。旧消息只补缺失节点和注释，自定义文本及空值不覆盖。默认兼容构建（Paper 1.21.11 API）与 Paper 26.2 Profile 各 306 项测试通过；同一成品已在 Paper 26.1.2 Build 70、26.2 Build 111 与真实 SQLite 验证失败回滚、队列继续运行及无需重载重试成功，26.2 狼骑乘回归也通过。未做真人客户端、真实 MEG 或 Folia 实服验证；详情见 [0.27.8 召唤无响应修复](更新日志-2026-09-01-召唤无响应修复.md)。

## 上一版 0.27.7 骑乘名牌与视角转向

停服备份后替换为 `LiPet-0.27.7-SNAPSHOT.jar` 并重启，执行 `/lipet status` 核对实际版本。已有 `nameplate.hide-while-ridden` 与其他自定义配置、注释保留：`true` 在骑乘时对所有玩家隐藏名称，下马恢复；`false` 继续显示。后续只调整这一开关可执行 `/lipet reload`。

本版上马立即启动连续控制，每 tick 读取当前视角和按键。默认兼容构建（Paper 1.21.11 API）与 Paper 26.2 Profile 各 264 项测试通过；同一 Jar 在 Paper 26.1.2 / 26.2 完成狼骑乘原生玩家探针，并在 26.2 追加蝙蝠骑乘探针。没有把这些结果视为真人客户端、真实 MEG 或 Folia 实服验证。配置与完整结果见 [0.27.7 骑乘名牌与视角转向](更新日志-2026-09-01-骑乘名牌与视角转向.md)。

## 上一版 0.27.6 货币名称配置与热重载

首次升级时停服备份后替换为 `LiPet-0.27.6-SNAPSHOT.jar`，保留原配置并重启一次。旧配置只补缺失节点与注释：`currency.playerpoints.display-name` 默认“点券”，`currency.vault.display-name` 默认“金币”，内置货币默认“宠物币”；管理员已有名称、注释与商品价格不覆盖。

完成升级后修改 `config.yml` 的三个 `currency.*.display-name`，执行 `/lipet reload` 并等待成功，再关闭并重新打开 `/lipet shop`、`/lipet itemshop` 即可。已经打开的菜单不会自动重绘。失败或被拒绝的并发重载不发布候选名称；服务器 ID、群组和内置货币初始余额仍按启动时读取。

默认构建与 Paper 26.2 Profile 各 216 项测试通过；同一 Jar 在 Paper 26.1.2 / 26.2 + PlayerPoints 3.3.5 完成服务端双商城 Lore、真实重载与扣款探针，未做真人客户端视觉测试。配置、步骤和完整验证见 [0.27.6 货币名称与热重载](更新日志-2026-08-31-货币名称重载.md)。

## 上一版 0.27.5 新增配置与行为

使用 `LiPet-0.27.5-SNAPSHOT.jar`，停服备份后替换，启动后执行 `/lipet status`。旧配置只补缺失项；等级模型示例默认禁用，填写真实蓝图后启用。206 项测试通过，Paper 26.1.2/26.2 已完成服务端启动、重载和捕捉球/SQLite 检查，客户端 MEG 视觉尚未实测。

完整示例见 [等级模型、随机属性与捕捉球商店](等级模型与捕捉球商店.md)。

## 2. 安装

1. 将 `LiPet-0.27.17-SNAPSHOT.jar` 放入服务器 `plugins/` 目录。
2. 启动服务器一次，让插件生成默认配置。
3. 停服，编辑 `plugins/LiPet/` 下的配置文件。
4. 再次启动服务器。
5. 执行 `/lipet status` 检查状态。

需要使用 CraftEngine 自定义物品时，安装 CraftEngine 26.8.1 后可执行 `/lipet status <CE物品ID>` 验证生成与反向识别链路；未安装 CraftEngine 时原版物品功能仍可使用。

需要使用按等级解锁的外部技能时安装兼容的 MythicMobs 5.x，并在对应 `宠物类型/*.yml` 中启用技能规则。MythicMobs 为软依赖，不安装时其他宠物功能仍正常；执行 `/lipet status` 可查看挂钩状态。

单服推荐 SQLite。群组服推荐 MySQL，并按需开启 Redis 同步。

## 22. 升级建议

升级前：

1. 停服。
2. 备份 `plugins/LiPet/`。
3. 备份 SQLite 或 MySQL 数据。
4. 替换 Jar。
5. 启动服务器。
6. 执行 `/lipet status`。
7. 检查控制台是否有 LiPet 警告或错误。

LiPet 会补全新增配置节点，但不会覆盖已有自定义配置值。

升级到 `0.27.11` 必须替换 Jar 并完整启动；`/lipet reload` 只能重读配置，不能为旧 Jar 加入经验批处理。启动后检查原版食物补全日志和烈焰人文件；已有 `growth.foods` 的文件由管理员负责，插件不会强改。

升级到 `0.27.9` 必须替换 Jar 后重启。旧配置自动补缺失消息与疾跑倍率；已有商品价格、支付 ID、宠物数值和管理员自定义文本保持不变。后续修改这些配置可执行 `/lipet reload`，已经打开的商城需要关闭后重新打开。

升级到 `0.27.6` 必须首次替换 Jar 并重启，旧版 `/lipet reload` 不能加载新代码。后续仅修改货币名称无需再次重启，重载成功并重开菜单即可；`shop.yml` 继续使用 `INTERNAL`、`VAULT`、`PLAYERPOINTS` 支付 ID，`100.00` 价格格式和实际扣款不变。

升级到 `0.27.4` 后，`capture.yml` 和 `pet-types.yml` 会补入默认关闭的 CraftEngine 捕捉球与宠物食物案例。替换其中的 `yourpack:*` 完整 ID 并启用后执行 `/lipet reload`；关闭案例不会要求服务器安装 CraftEngine，也不会覆盖原有捕捉球和食物数值。

升级到 `0.27.2` 后，数据库中的 `bat`、`fox`、`minecraft:bat` 等旧原版类型 ID 会自动兼容到当前 `vanilla_<实体名>` 类型。插件还会为商城现有 10 种宠物创建缺失的独立 YML；已有文件、类型、数值和注释不会覆盖。

升级到 `0.27.1` 后，历史官方 `status-mythicmobs` 默认文本会自动加入技能类型数和规则数变量；服主自定义状态文本与注释保持不变。MM 失败技能新增退避与逐技能日志限频，不需要改现有技能规则。

升级到 `0.27.0` 后，旧宠物类型文件会增加一个带完整中文注释、`enabled: false` 的 MM 技能示例；它不会调用占位技能。配置真实技能名并启用后，执行 `/lipet reload` 即可生效。

从旧版升级时，如果 `宠物类型/` 尚不存在，插件会把 `pet-types.yml` 的旧 `types` 按类型拆成多个文件并保留注释；如果目录已经存在，则不会移动或覆盖，旧单文件格式仍可继续读取。

## 23. 构建

普通通用包：

```text
mvn clean package
```

Paper 26.2 编译检查：

```text
mvn -Ppaper-26.2 clean package
```

输出：

```text
target/LiPet-0.27.17-SNAPSHOT.jar
```

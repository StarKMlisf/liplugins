# LiPet 更新日志

当前版本：`0.27.9-SNAPSHOT`。

## 2026-09-02 · 0.27.9 · 商城、飞行加速与回收

- 道具商城余额不足改用独立的 `item-shop-insufficient-funds`，不再显示“无法购买宠物”；扣款后的发放任务受理后退休也会结束并退款。
- 基础速度、敏捷和技能加速同时写入实体支持的地面/飞行速度属性；骑乘实时读取疾跑键，并按可配置的 `riding-sprint-multiplier` 放大水平与飞行俯仰速度。
- 命令、管理 GUI 和信号棒统一回收反馈；超过 100 tick 提示正在保存，反馈调度失败有日志和独立文本，数据库保存安全顺序保持不变。
- 默认兼容构建与 Paper 26.2 Profile 各 312 项测试通过；同一最终 Jar 在 Paper 26.1.2 Build 70、26.2 Build 111 各完成 8 轮真实 SQLite 回收，并验证鹦鹉飞行属性及疾跑倍率。

配置、升级和验证记录见 [2026-09-02 商城、飞行加速与回收更新](更新日志-2026-09-02-商城飞行加速与回收.md)。

## 2026-09-01 · 0.27.8 · 召唤无响应修复

- 修复实体任务已受理、却在执行前退休时未结束 Future，导致同一玩家的召唤与后续操作长期排队的问题；旧版配置重载不会重建这条运行时队列。
- 执行、调度拒绝、退休与同步异常统一结束任务；召唤失败按本次最新已保存的 revision 回滚并释放租约，避免首次外观保存后使用旧版本号恢复失败。首个 ACTIVE 写入冲突或同步/异步失败时只释放租约，不猜测版本号覆盖其他记录。
- 玩家操作队列不在 Map 原子更新中执行业务；同步依赖错误不会毒化后续队列，取消公开返回值不会提前放行仍在进行的数据写入。
- 合并查询的占位在成功、失败及同步错误后释放，避免查询失败后永久复用一个未完成的结果。
- 命令与仓库统一反馈；等待超过 100 tick 仅提示一次，可通过 `messages.yml -> pet-call-pending: ""` 关闭。提示异常与业务失败分开说明，不会因提示出错误报已经成功的召唤。
- 默认兼容构建（Paper 1.21.11 API、Java 21 字节码）与 Paper 26.2 Profile 各 306 项测试通过，失败、错误和跳过均为 0。同一最终 Jar 在 Paper 26.1.2 Build 70、26.2 Build 111 与真实 SQLite 验证受理后退休、失败回滚到 STORED revision 4、队列继续运行及无需重载重试成功；26.2 狼骑乘回归也通过。未做真人客户端、真实 MEG 画面或 Folia 实服验证。

升级、配置和完整记录见 [2026-09-01 召唤无响应修复](更新日志-2026-09-01-召唤无响应修复.md)。0.27.7 的骑乘名牌与持续视角控制修复继续保留。

## 2026-09-01 · 0.27.7 · 骑乘名牌与视角转向

- 骑乘隐藏名牌时对骑手及旁观者统一生效，清空载体原版名称；下马恢复，同一个 TextDisplay 不因普通骑乘隐藏而删除重建。
- 上马立即启动每 tick 控制，按骑手实时视角与当前按键转向，覆盖原地转头及持续按住前进键的情况。
- 下马、退出和失效骑乘清理控制任务；单行回退也遵守隐藏规则，已有自定义配置保留。
- 骑乘临时暂停原版感知/方向控制，保留实体 AI 标志与物理动作；下马、异常或换骑恢复原状态。跟随、战斗和延迟回传会检查嵌套座位骑乘，避免抢方向。
- 重载保留仍在骑乘的感知/坐下状态；管理员在骑乘中调整 AI 或坐下时，不以旧快照覆盖其选择。
- 默认兼容构建（Paper 1.21.11 API）与 Paper 26.2 Profile 各 264 项测试通过，失败、错误、跳过均为 0。同一 Jar 在 Paper 26.1.2 Build 70 的狼骑乘、26.2 Build 111 的狼及蝙蝠骑乘探针均通过，覆盖名牌、连续转向、重载和多层座位；本轮未验证真人客户端视觉、真实 MEG 或 Folia 运行。

配置及本版验证记录见 [2026-09-01 骑乘名牌与视角转向更新](更新日志-2026-09-01-骑乘名牌与视角转向.md)。

## 2026-08-31 · 0.27.6 · 货币名称配置与热重载

- `config.yml` 新增 PlayerPoints / Vault 显示名称，默认“点券 / 金币”，内置货币默认“宠物币”。
- 三种名称在 `/lipet reload` 成功后统一更新；关闭再打开两个商城即可看到新名称，已经打开的菜单不自动重绘。
- 失败或被拒绝的并发重载不发布候选名称；旧配置仅补缺失节点与注释，自定义值和注释保持不变。
- `shop.yml` 支付 ID 仍为 `INTERNAL`、`VAULT`、`PLAYERPOINTS`；价格 `100.00`、余额、结算规则和启动时读取的服务器身份/群组/初始余额不变。
- 默认构建与 Paper 26.2 Profile 各 216 项测试通过，失败、错误、跳过均为 0。同一 Jar 在 Paper 26.1.2 / 26.2 + PlayerPoints 3.3.5 已验证服务端双商城 Lore、真实重载和扣款；这不等于真人客户端视觉实测。

配置示例、升级步骤与验证范围见 [2026-08-31 货币名称与重载更新日志](更新日志-2026-08-31-货币名称重载.md)。

## 2026-08-30 · 0.27.5 · 等级模型与随机捕捉

- ModelEngine 按等级门槛更换模型；原版捕捉四属性随机并持久保存。
- 道具商城可通过 `item.capture-ball` 发放真实捕捉球，附普通及 CraftEngine 配置案例。

详情见 [2026-08-30 等级模型与随机捕捉更新日志](更新日志-2026-08-30-等级模型与随机捕捉.md)。

## 历史记录 · 2026-08-26

适用版本：`0.26.10-SNAPSHOT`

今天的更新由 `0.26.8`、`0.26.9` 和 `0.26.10` 三次迭代组成，集中解决外部模型显示、CraftEngine 物品喂食以及指令冲突问题。

## 更新摘要

| 版本 | 主要改动 | 服主需要注意 |
| --- | --- | --- |
| `0.26.8` | 修复 ModelEngine / MEG 模型与原版宠物载体重叠 | 外部模型建议开启 `hide-base-entity` |
| `0.26.9` | 支持使用 CraftEngine 自定义物品喂食 | CE 食物必须填写完整 `namespace:item_id` |
| `0.26.10` | 删除 `/lp` 短指令别名 | 玩家和后台统一使用 `/lipet` |

## 0.26.8：ModelEngine / MEG 模型重叠修复

使用 ModelEngine 模型后，LiPet 仍然需要一个原版实体承载 AI、寻路、属性和交互。旧实现没有可靠隐藏这个载体，因此会出现模型和原版宠物同时显示。

本次更新加入：

- 使用 ModelEngine 载体可见性接口隐藏原版实体外观，不使用隐身药水伪装。
- 隐藏外观不会关闭宠物 AI、感知、寻路和战斗能力。
- 重复召唤、区块重新加载和运行状态恢复时复用已有模型，避免重复叠加。
- `/lipet reload` 后刷新已召唤宠物的模型配置。
- 修改模型 ID 时，先卸载本轮运行期记录的旧模型，再挂载新模型。
- 模型载体隐藏和模型碰撞箱覆盖可以分别控制。

推荐配置：

```yaml
types:
  wolf:
    model:
      provider: "MODEL_ENGINE"
      id: "magecat"
      # 隐藏原版载体外观，但保留 LiPet AI 与寻路。
      hide-base-entity: true
      # 使用 ModelEngine 蓝图碰撞箱。
      override-hitbox: true
```

如果仍然出现原版宠物：

1. 确认 `provider` 为 `MODEL_ENGINE`。
2. 确认 `hide-base-entity: true`。
3. 执行 `/lipet reload`，再重新召唤宠物。
4. 检查 ModelEngine 是否为支持当前 Paper 26.x NMS 的更新构建。

## 0.26.9：CraftEngine 自定义物品喂食

宠物食物现在同时支持：

- 原版 Bukkit Material，例如 `BEEF`、`COOKED_BEEF`。
- CraftEngine 完整自定义物品 ID，例如 `default:pet_biscuit`。
- ID 中含点号的 CraftEngine 物品，例如 `my.pack:pet.food`。

配置示例：

```yaml
types:
  wolf:
    foods:
      beef:
        material: "BEEF"
        display-name: "生牛肉"
        experience: 20
        healing: 6.0

      pet-biscuit:
        # 必须填写完整命名空间 ID，不能只写 pet.food。
        item-id: "my.pack:pet.food"
        display-name: "灵宠饼干"
        experience: 35
        healing: 8.0
        attribute-points: 0
        minimum-level: 1
        maximum-level: 0
```

识别规则：

- CraftEngine 物品按完整 ID 精确匹配，不按底材 `Material` 猜测。
- 即使 CE 物品和原版物品都使用同一种底材，也只会消耗配置中指定的物品。
- 简单 ID 可以直接作为食物节点名；ID 含 `.` 时使用普通节点名并填写 `item-id`。
- 未安装 CraftEngine 时不会禁用 LiPet，原版食物继续正常使用。
- CraftEngine 重载物品后无需重启 LiPet；插件每次交互都会读取当前物品 ID，不缓存旧物品实例。
- 不满足等级要求、没有产生有效效果或数据保存失败时，不会吞掉玩家物品。

## 0.26.10：取消 `/lp` 指令别名

LiPet 现在只注册 `/lipet` 主指令。

已移除：

- `/lp`
- `/lipet:lp`

保留：

- `/lipet`
- Bukkit 命名空间指令 `/lipet:lipet`
- 原有全部 `/lipet` 子指令、权限和 Tab 补全

这样不会再与 LuckPerms 等权限插件常用的 `/lp` 主指令发生冲突。旧菜单、NPC 或脚本如果仍在执行 `/lp`，需要改成 `/lipet`。

## 兼容与验证记录

| 检查项 | 结果 |
| --- | --- |
| Paper `26.1.2` Build 70 | 通用 Jar 启动与功能探针通过 |
| Paper `26.2` Build 111 | 通用 Jar 启动、指令与安全关闭通过 |
| ModelEngine R4.1.0 更新构建 | `magecat` 模型载体隐藏、幂等恢复和卸载恢复通过 |
| CraftEngine `26.8-SNAPSHOT` | CE 完整 ID 与同底材原版物品区分通过 |
| `0.26.8` 自动测试 | 68 项通过 |
| `0.26.9` 自动测试 | 72 项通过 |
| `0.26.10` 自动测试 | 73 项通过 |

ModelEngine 即使显示版本相同，不同构建支持的 NMS 版本也可能不同。若出现 `Unsupported NMS Version`，需要升级 ModelEngine，而不是关闭 LiPet 的宠物 AI。

## 升级步骤

1. 停服并备份 `plugins/LiPet/`、SQLite 文件或 MySQL 数据库。
2. 删除旧 LiPet Jar，只保留 `LiPet-0.26.10-SNAPSHOT.jar`。
3. 启动服务器，等待配置缺失节点自动补齐。
4. 执行 `/lipet status`，确认插件版本、数据库目标和外部 Hook 状态。
5. 使用 MEG 模型时检查 `hide-base-entity` 与 `override-hitbox`。
6. 使用 CE 食物时检查完整 `item-id`，再用同底材原版物品做一次反向测试。
7. 检查菜单、NPC 和脚本，确保没有继续调用 `/lp`。

不要使用插件管理器热卸载或热加载 LiPet。普通配置可使用 `/lipet reload`，更换 Jar 必须完整重启服务器。

## 上线前快速检查

- 召唤 MEG 宠物后只显示模型，不显示脚下原版实体。
- 宠物仍能跟随、绕障、攻击、召回和收回。
- CE 食物可增加经验或生命，未配置的同底材物品不会被消耗。
- `/lipet` 正常，`/lp` 交还给 LuckPerms 或显示未知指令。
- 重启服务器后宠物、背包、属性和货币数据仍然存在。

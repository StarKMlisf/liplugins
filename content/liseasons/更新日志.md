# 更新日志

## 1.0.80 · 动态层次与定向音景（2026-09-18）

- 龙卷风新增地面进气带、阵风脉动和抬升碎屑，保持移动/贴地，不扩大风力范围。
- 雷暴新增暗云蓄电和间歇连闪；蘑菇云新增预警环、一次性扩散冲击环、上升翻卷与后段冷却灰烟，不再只是重复同一帧轮廓。
- 流星新增独立尺寸变化、亮核脉动、分叉火星和燃尽烟尾；分叉纯视觉，不增加实体、落点或奖励。
- 四种原生效果新增独立 `atmosphere` 配置，次级细节共享原预算。音景仅发给触发玩家，随距离/阶段衰减，整场限频；可单独静音。
- 保留创造模式安全预览、无地形破坏、原有伤害/概率/奖励、Folia玩家线程发送与离线/重载回收。165项单元测试通过；实际听感、画面与客户端帧率仍待真人验收。
- 验证：隔离Folia26.2有MM/ME和无MM/ME环境各64项代理玩家检查通过，包含真实粒子载荷、预警纯展示、音轨冷却、燃尽烟尾、安全预览与清理；不等于真人视觉或听感验收。

## 1.0.79 · 大型参考图粒子与免疫玩家预览（2026-09-18）

- 龙卷风视觉高度/宽度独立放大，默认约64格高、顶部直径32格，576颗/帧；保持移动、贴地与原伤害范围，远距发送避免云顶被默认距离裁剪。
- 新增原生多道分叉闪电和56格火焰蘑菇云，包含烟柱、翻卷帽盖、地面尘环，不调用真实爆炸、不改方块。
- 流星改为明亮火球与48格橙色长尾；新默认缩短发射间隔、延长飞行展示，每场数量、坠地概率与奖励不增加。上述四种原生模式不叠加旧ME模型。
- 修复创造/旁观/无敌/免疫玩家手动触发却没有展示：明确进入安全预览，不伤害、不施加温差、不卷吸、不生成灾害生物；自然触发继续尊重免疫。
- 场景按玩家实体线程发送，排队去重、轮转分帧、分组全服粒子预算；结束、离线、切世界、重载回收。新配置带中文范围注释，旧自定义值不自动覆盖。
- 服务端测试不等于真人视觉验收；模组参考图只用于形态和规模，不承诺逐像素一致。
- 验证：158项 Maven 测试通过；Folia 26.2 有MM/ME与无MM/ME两种环境各58项代理玩家检查通过，包含真实粒子载荷调用、免疫玩家预览安全、位移、模型不叠加与清理。无前置环境使用放大后的新默认配置。

## 1.0.78 · 龙卷风改为原生云雾（2026-09-18）

- 本轮只重做龙卷风表现：原生 CLOUD/SMOKE 体积采样、上升螺旋、顶部云盘、尘埃和少量树叶破碎粒子；不拔树、不改方块。
- 默认启用 `appearance.mist`，不创建 ME 龙卷风，也不需要龙卷风资源包；旧蓝图保留但不调用，其他灾害模型不变。
- 风柱支持直线/缓弯行进，默认向前速度 0.65 格/秒，0 完全停止水平行进；旧速度不覆盖。视觉与风力使用同一移动中心，已加载地面在所属区域读取并平滑跟随，不追踪玩家瞬移。
- 中文配置提供密度倍率、全服粒子上限、碎叶开关。默认 144 颗/帧、全服 4096 颗/秒，超限仅削减视觉，不削减保护或物理判定。
- 沿用玩家实体线程、屋顶/独立晴天保护、预警无伤害、非致死不卷吸，以及结束/离线回收。共享预算轮换首位，避免固定玩家先占满。
- 验证：152 项 Maven 测试通过；Folia 26.2 安装/不安装 MM/ME 两种环境分别通过 47 项代理玩家检查，覆盖粒子数据、风柱位移、保护与清理。包括并发预算、几何边界、移动轨迹、静止配置、贴地插值与区域采样生命周期。真人客户端的云雾密度、帧率、受力仍需进服验收。

详见[自然灾害与龙卷风](自然灾害与龙卷风.md)。

## 1.0.77 · 无损灾害扩展与精细模型（2026-09-18）

- 新增地震、蘑菇云、天坑、海啸和尸潮，共 13 种持续灾害、25 种季节事件、15 个原创 MM/ME 模型；原九模型细化几何、材质与动画。
- 默认不破坏方块：裂缝、天坑、浪墙、卷起的碎石均为模型；天坑不挖洞，海啸不放水，蘑菇云不调用真实爆炸。
- 雷暴增加有全服预算的原生闪电效果；龙卷风可卷起限量自然敌对怪，排除命名、插件实体及宠物，非致死模式禁用卷怪。
- 尸潮每波 3、每人 8、全服 32，最多存活 45 秒，无经验、无掉落、不破门、不增援，结束与离线回收。蘑菇云、尸潮默认自然概率 0，仅手动触发。
- 新增完整中文 `disaster-behavior.yml`，领地查询失败安全跳过；Folia 分区扫描、实体线程操作与配额包括排队任务。
- 验证：138 项 Maven 测试、15 模型的 3980 项几何/动画数据校验、Folia 26.2 + MM 5.13 + ME R4.1.0 的 162 项加载/生命周期/尸潮检查通过。真人客户端视觉、实战受力与实际领地插件组合仍需进服验收。

详见[自然灾害与龙卷风](自然灾害与龙卷风.md)。

## 1.0.76 · MythicMobs / ModelEngine 灾害模型（2026-09-18）

- 新增九个原创 Blockbench 模型：旋转龙卷风、岩核火尾流星，以及冰雹、沙尘暴、冻雨、暴雪、热浪、寒潮、雷暴；内置贴图与循环动画，配套 MM 纯展示载体。
- 模型配置独立到 `model-display.yml`，支持总开关、各模型 ID/动画/尺寸、每人及全服配额。MM/ME 为可选运行时前置，不打包商业依赖，不自动下载商业插件；缺失时保留粒子和原有业务。
- 分离灾害判定、模型生命周期、外部插件桥接与实体调度。Folia 归属区域生成、载体线程更新、异步移动；结束、离线、死亡、切世界和重载回收，并拦截过期的排队生成。MM 使用立即注销，避免载体退休后延迟注销失效。
- 流星所有玩家粒子发送路径统一检查运行时所需数据，补齐 Color、DustOptions、DustTransition，避免辅助火花缺少 Color 导致整颗流星中断。
- 补齐温度提示缓存的玩家离开清理，以及温度快照、融雪队列在重载/停用时的清理。现有群系染色已经是有预算的 PacketEvents 客户端发送，不恢复旧版大范围 getBiome 扫描。
- 模型不改变保护、伤害或奖励逻辑；客户端必须加载正式服 ME 生成并合并后的资源包。服务端导入及生命周期测试不等同于真人客户端视觉验收。
- 验证：134 项 Maven 测试通过；Folia 26.2 + MM 5.13 + ME R4.1.0 隔离服 80 项检查通过，包含九模型导入/挂载/销毁、配额、排队取消、跨区域移动、离线回收、失联释放与真实服务端流星粒子调用。

详见[自然灾害与龙卷风](自然灾害与龙卷风.md)。

## 1.0.75 · 冰雹、沙尘暴与冻雨（2026-09-15）

- 持续灾害由 5 种扩展到 8 种，季节事件由 17 种扩展到 20 种；新增春季冰雹、秋季沙尘暴、冬季冻雨，各默认 2% 检查概率、1200 秒个人冷却。
- 冰雹：冰屑粒子、降温及周期撞击伤害。沙尘暴：棕色风沙粒子、虚弱及轻微升温。冻雨：下落水滴、降温、减速和安全冻屏。
- 继续使用预警、增强、消散与个人灾害机制；屋顶和个人晴天可屏蔽新灾害。默认不生成实体、不改地形、不损坏建筑，不影响其他玩家或宠物。
- 新增完整中文 `visual` 配置，粒子与伤害使用独立节流；渐变时自动减少粒子，坐标计算不扫描跨区域方块，数据型粒子携带正确参数。
- 现有命令 Tab 与日历自动列出 `hailstorm`、`sandstorm`、`freezing-rain`，无新增权限；旧配置只补缺失节点。
- Folia 26.2 隔离服 43 项代理检查通过，配置迁移保留 1909 项原值、新增 84 项；真人客户端效果和反作弊仍需进服验收。

详情与测试命令见[自然灾害与龙卷风](自然灾害与龙卷风.md)。

## 1.0.74 · 立体风柱与灾害演变（2026-09-15）

- 龙卷风默认改为原生立体粒子模型：高空云盘、弯曲旋转漏斗、地面尘屑环，共用原有每帧预算。无需客户端模组、额外资源包或 ModelEngine，不新增实体。
- 预警期间可看到云盘，但没有卷吸、温差或基础伤害；预警结束后漏斗逐渐向下成形，结束前收缩消散。
- 五种持续灾害新增 `evolution`：默认 8 秒增强、结束前 12 秒减弱；温差、基础脉冲伤害和龙卷风力按平滑曲线变化，短场次自动压缩过渡。配置药水等级和效果列表不缩放。
- 新增 `vortex.appearance` 完整中文配置，可关闭尘屑、预警云或回退旧版螺旋；自定义峰值和概率不被覆盖。
- 灾害效果执行后再次检查玩家身份、屋顶、个人晴天和范围，避免外部插件同步传送/重载后继续应用旧伤害。
- 仅借鉴 Weather2 的分层、半径与旋转表现思路，独立实现 Bukkit 代码；不打包或移植其 Forge 代码、贴图或模型。
- 122 项自动测试、Folia 26.2 隔离服 25 项服务检查通过；配置迁移保留 1892 项旧值，只新增 17 项。Java 21 字节码、不打包依赖。

模型仍只向触发玩家显示，路径高度固定，不是多人共享天气系统。真人客户端外观、受力、反作弊组合需要进服验收。详见[自然灾害与龙卷风](自然灾害与龙卷风.md)。

## 1.0.73 · 龙卷风（2026-09-13）

- 新增第 17 种季节事件 `tornado`，默认夏季 2% 检查概率、1200 秒个人冷却，支持现有命令 Tab 补全与日历列表。
- 10 秒预警后出现持续旋转、直线移动的漏斗风柱；范围内卷吸、抬升并周期受伤，潜行可减轻风力，屋顶、个人晴天及跑出范围可以避险。
- 默认只影响触发玩家，不改变方块、不影响宠物和掉落物。非致死模式关闭位移，避免间接摔死；普通致死模式保留原版摔落伤害。
- 中文配置位于 `climate-survival.yml -> disasters.TORNADO`，包含粒子数量、轮转预算、移动速度、范围、风力和抬升上限；旧配置只补缺失节点。
- Folia 分离全局轮转和玩家实体操作，风柱只用坐标计算，不读取途经区块；延迟帧校验状态，重载、离线、换季或替换灾害后不继续施加旧风力。
- 新增可取消的 `TornadoWindEvent`。114 项自动测试通过；Folia 26.2 隔离服 19 项代理玩家服务检查通过，真人受力和客户端风柱效果仍需进服验收。

升级 Jar 后完整启动服务器，在已启用季节的世界执行 `/seasons event trigger tornado` 验证。详情见[自然灾害与龙卷风](自然灾害与龙卷风.md)。

## 1.0.72 · 自然灾害与温度生存（2026-09-13）

- 热浪、寒潮、暴风雪和雷暴改为预警、持续影响、结束三阶段；屋顶降低温差并躲避脉冲，个人晴天可屏蔽暴风雪和雷暴。
- 新增累计暴露后的热衰竭、中暑、极热灼伤、失温、冻伤与极寒冻伤，默认允许致死；普通炎热不会直接着火。
- 新增完整中文 `climate-survival.yml`，可修改阈值、暴露时间、伤害、致死开关、效果及消息；升级保留旧配置自定义值。
- 安全温度下停止对应阶段伤害并逐渐消退暴露；死亡、重生、离线、切换世界及重载清理状态。创造/旁观/无敌、无季节世界与免疫权限跳过伤害。
- 伤害使用 Bukkit 事件链，点燃显式发送可取消的燃烧事件；非致死模式不点燃。冻屏限制在原版冻伤阈值以下，避免重复扣血。
- 体感与室内保温按时间渐变，菜单重复查询不加速；冷热源分别取最强值后相加，熄灭营火不再参与，Folia 只扫描已加载且当前线程拥有的区块。
- 最低 Paper 1.21.1 API 编译，105 项测试通过；Folia 26.2 隔离服加载、重载、关闭与 9 项服务代理探针通过。真人伤害、客户端效果及多保护插件组合仍需进服验收。

升级需完整停止服务器并替换 Jar；`/seasons reload` 只重读配置，不能换掉旧代码。游戏体感阈值不是现实医学标准，详见[体温系统](体温系统.md)。

## 1.0.71 · Folia 秋季树叶边界修复（2026-09-05）

- 修复玩家位于世界最低高度以下或最高高度以上时，秋季真实树叶随机采样形成反向区间并抛出 `bound must be greater than origin` 的问题。
- 采样中心高度现在先限制到当前世界合法范围，普通高度下的原有搜索范围保持不变。
- Folia 下按目标区块分组并交给对应区域线程读取、备份和替换树叶，避免玩家实体任务跨区域直接访问方块。
- 延迟区域任务会再次检查插件状态、配置快照、季节和区块加载状态，重载或换季后不会继续执行旧任务。
- 新增世界上下边界与异常半径回归测试；90 项测试全部通过。Folia 1.21.11 与 Folia 26.2 实际加载、重载和正常关闭均通过，未出现 LISeasons 任务异常。

请停服替换为 `LISeasons-1.0.71.jar` 后完整启动。无需删除配置文件，仅执行 `/seasons reload` 不能替换旧版类文件。

## 1.0.70 · Folia 流星雨粒子修复（2026-09-02）

- 修复 Paper/Folia 1.21.10+ 中 `FLASH` 粒子改为强制要求 `org.bukkit.Color` 后，流星第一帧报 `missing required data class org.bukkit.Color` 并全部终止的问题。
- 运行时按服务端真实 `Particle#getDataType()` 自动选择：新版补颜色、Paper 1.21.1 旧契约继续传空数据。
- 配置中的其他数据型粒子不兼容时自动回退安全粒子，避免一项错误配置让整颗流星和后续落点流程消失。
- 保留现有 `season-events.yml` 自定义值；新增中文注释说明允许范围。
- 85 项回归测试通过；Folia 1.21.11 成品运行验证确认 `FLASH -> Color`，带色闪光和流星落点渲染均成功。

请停服替换为 `LISeasons-1.0.70.jar` 后完整启动。仅执行 `/seasons reload` 无法替换旧版类文件。

## 1.0.69 · Residence 与安全积雪（2026-08-31）

### 修复与兼容

- 冬季结冰、常规铺雪、暴风雪铺雪接入 Bukkit `BlockFormEvent`，春季融化接入 `BlockFadeEvent`，遵守 Residence 与其他保护插件的取消结果。
- Residence 的 `iceform` 控制雪层和冰生成，`icemelt` 保护雪层、普通冰和霜冰。雪块/细雪/浮冰/蓝冰的保留请使用 LISeasons 材质融化开关，避免误解 Residence 的原生保护范围。
- 铺雪使用原版支撑检测；支持上半台阶、双台阶和倒置楼梯，跳过下半台阶、普通楼梯等无合适顶面的方块，不覆盖建筑。
- 修复暴风雪用地表方块本身判断空气导致积雪失败；现在从表面上方一格尝试。
- 暴风雪默认尊重玩家独立天气，兼容领地晴天；普通水雪循环仍独立工作，可由领地 `iceform` 禁止。
- Folia 的随机铺雪、结冰、近身融雪按目标方块区域调度，执行前复查配置/季节/区块。
- 事件中已被其他插件修改的方块不会再次覆盖；保留受保护雪层下的草方块雪态。

### 配置与升级

新增 `season-world-rules.snow-accumulation.allow-top-slabs`、`allow-top-stairs`、`allow-leaves` 和 `visual-effects.respect-player-weather`，均有中文用途与取值说明。升级仅补全缺失节点/注释，保留原配置与玩家建筑。

停服备份后替换 `LISeasons-1.0.69.jar`，无需删除配置文件。PacketEvents 2.13.0+ 仍为强制前置；Residence/CMILib 可选，未安装不影响 LISeasons。

### 验证范围

81 项单元测试通过；Paper 1.21.1 与 Folia 1.21.11 搭配 Residence 6.0.0.1 + CMILib 1.5.8.1，分别通过 31 项冰雪/领地/方块支撑/区域线程实服检查。未进行真实客户端视觉验收或长期多人压测，详见[构建与兼容记录](开发构建与版本兼容.md)。

原版不能让雪层与下半台阶共用一格。本版不提供“贴面雪皮”模拟；完整规则和配置见[季节效果与世界规则](季节效果与世界规则.md)。

## 1.0.68 · 季节增益栏与陨石奖励

- 新增可配置的季节 Buff BossBar，并与 `/seasons hud off` 联动。
- 陨石奖励支持安全散落与弹起速度，单颗流星只结算一次落点奖励。
- 修复 Folia 控制台世界信息查询的区域线程问题，64 项回归测试通过。

[返回首页](Home.md)

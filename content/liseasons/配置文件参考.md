# 配置文件参考

本页用于快速定位配置；每个默认 YAML 文件本身也带有逐项中文注释。详细机制请点击对应专题页。

## 文件总表

| 文件 | 主要内容 | 支持 `/seasons reload` |
| --- | --- | --- |
| `config.yml` | 时间、世界、季节规则、视觉、冰雪、生物、集成 | 是 |
| `messages.yml` | 命令、通知、HUD、GUI 文本、名称映射 | 是 |
| `temperature.yml` | 体温全系统 | 是 |
| `season-color.yml` | NMS 季节色调 | 是 |
| `season-events.yml` | 季节事件与流星雨 | 是 |
| `festivals.yml` | 节日 | 是 |
| `gui.yml` | GUI 全局兜底 | 是 |
| `gui/*.yml` | 六个独立菜单 | 是 |
| `calendar-state.yml` | 服内日历运行状态 | 由插件维护 |

## `config.yml`

### `time`

| 键 | 默认 | 说明 |
| --- | --- | --- |
| `use-gregorian-trigger` | `false` | 是否使用服务器现实日期 |
| `sync-real-time` | `false` | 是否同步所有已启用 NORMAL 世界的游戏时钟到现实时间 |
| `real-time-sync-interval-seconds` | `1` | 实时时钟同步周期 |
| `refresh-interval-seconds` | `60` | 季节自动刷新周期，实际最小 10 秒 |
| `world-calendar.enable` | `true` | 非公历模式下启用持久化服内日历 |
| `world-calendar.start-year` | `2026` | 起始年份 |
| `world-calendar.start-date` | `02-04` | 起始月日 |
| `world-calendar.start-world-day` | `0` | 日历锚点世界天数 |
| `world-calendar.world-days-per-calendar-day` | `1` | 每个日历日需要的世界天数 |
| `season-start-dates.*` | 03-04/06-04/09-04/12-04 | 四季开始日期 |
| `season-duration-days.*` | `24` | 旧版循环模式每季天数 |
| `gregorian-dates.*` | 24 节气表 | 节气公历日期 |

详见[日期、季节与二十四节气](日期-季节与二十四节气.md)。

### `worlds`

- `enable-all`：是否默认启用所有世界；
- `allowed`：显式允许的世界名；
- `denied`：显式拒绝的世界名，优先级最高。

### `biome_settings.frozen_biome`

- `enable`
- `lock_winter_mode`
- `disable_ice_melting`
- `disable_snow_melting`
- `target_biomes`

### `messages` 与 `effects`

- `messages.broadcast-change`：换季/换节气广播；
- `messages.join-notify`：加入时提示；
- `effects.change-particle.*`：变化时玩家粒子；
- `effects.change-sound.*`：变化时玩家声音。

### `integration`

- `customcrops.enable`：尝试注册 CustomCrops 季节提供器；
- `dominion.protect-ice-and-snow`：保护领地内冰雪。

### `visual-effects`

- `interval-ticks`、`search-radius`
- `aurora.enable/chance-percent`
- `blizzard.enable/snow-placement-chance`
- `falling-leaves.enable`
- `autumn-leaf-color.enable/radius/budget-per-player`
- `tree-snow.enable`
- `apple-drop.enable/chance-percent`

1.0.61 极光实际使用五天一次门槛，`chance-percent` 暂未被渲染器读取。

### `season-effects.<season>`

- `uncovered-crop-growth-multiplier`
- `uncovered-crop-stop`
- `rain-chance`
- `thunder-chance`

1.0.61 的天气服务在冬季直接保留原版/管理员天气，不读取冬季雨雷概率。

### `season-world-rules.water-cycle`

- 总开关、周期、搜索范围；
- 冬季结冰、铺雪与春季融化概率；
- 换季融雪半径、列预算、方块读取预算、队列上限、区块刷新上限；
- `meltable-blocks` 方块到结果的映射；
- 结冰与融化各自的群系排除关键词。

### `season-world-rules.mob-rules`

- `enable`
- `summer-zombie-to-husk-chance-percent`
- `winter-skeleton-to-stray-chance-percent`

详见[季节效果与世界规则](季节效果与世界规则.md)。

## `messages.yml`

主要节点：

| 节点 | 用途 |
| --- | --- |
| `prefix` | 插件前缀 |
| `season-names` | 纯季节名 |
| `season.switch/case/default` | 带颜色季节映射 |
| `world.switch/case/default` | 带颜色世界映射 |
| `solar-term-names` | 24 节气显示名 |
| `command.*` | 权限、错误、设置、查询和帮助文本 |
| `notify.*` | 加入、变化、标题、体温和事件通知 |
| `hud.*` | ActionBar 文本 |
| `calendar.*` | 旧版 GUI 文案兜底 |

文本使用 MiniMessage。`<world>`、`<season>` 等上下文占位符由插件替换；未知标签或未闭合标签可能导致显示异常。

## `temperature.yml`

主要节点：

- `season-base-temperature`
- `base-temperature.mode/season`
- `solar-term-modifier`
- `biome-vanilla-scale`
- `tropical-biomes`
- `biome-overrides`
- `condition`
- `water`
- `armor`
- `items.held`
- `items.consumables`
- `body-temperature-step`
- `display-decimals`
- `action-bar`
- `notice`
- `event-interval-ticks`
- `temperatureEventsEnabled`
- `temperature-event-mode`
- `temperature-event-cooldown-seconds`
- `temperatureEvents`

详见[体温系统](体温系统.md)。

## `season-color.yml`

主要节点：

- `season-switch`
- `interval-ticks`
- `biome-spoof.radius-chunks`
- `biome-spoof.budget-chunks-per-tick`
- `biome-spoof.step-xz/step-y`
- `biome-spoof.nms.enable`
- `biome-spoof.nms.max-chunks-per-packet`
- `biome-spoof.seasons`

旧版 `enable` 字段不再作为当前 NMS 总开关读取。详见[NMS 季节染色](NMS季节染色.md)。

## `season-events.yml`

主要节点：

- `global.event-cooldown-seconds`
- `global.check-interval-seconds`
- `global.highest-priority-only`（1.0.61 当前未读取；管理器本身每轮只选择第一个可触发事件）
- `meteor-shower`：世界、飞行、外观、颜色、落点、奖励；
- `spring-events`
- `summer-events`
- `autumn-events`
- `winter-events`

详见[季节事件与流星雨](季节事件与流星雨.md)。

## `festivals.yml`

- `global.festival-cooldown-seconds`（1.0.61 当前未读取）
- `global.enabled`（1.0.61 当前未读取）
- `server-custom-festivals.<id>`

要关闭节日，应逐项设置 `server-custom-festivals.<id>.enabled: false`。节日使用服务器现实日期。详见[节日系统](节日系统.md)。

## `gui.yml` 与 `gui/*.yml`

页面主要结构：

```yaml
menu:
  title: "<gradient:...>标题</gradient>"
  rows: 6
  size: 54
  layout: []
  icons: {}
  items: {}
```

独立页面优先，`gui.yml` 只作兜底。详见[日历 GUI](日历GUI.md)。

## `calendar-state.yml`

由插件按世界 UUID 自动保存。要校准时：

1. 停止服务器；
2. 备份文件；
3. 修改 `elapsed-calendar-days`、`world-day-remainder`、`last-world-day`；
4. 不要只修改只读展示用的 `date`；
5. 启动后用 `/seasons info <world>` 验证。

[返回首页](Home.md)

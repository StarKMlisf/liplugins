# 更新日志

## 1.1.0 - 2026-08-08

- 升级到 Paper 26.1.2 与 Java 25。
- 重构为主类、命令、配置、监听器、业务、数据、工具和 API 分层。
- 修复每日次数重启清零和系统默认时区导致的错误重置时间。
- 新增可配置时区、每日重置时刻、倒计时、准确重置时间和 PAPI 变量。
- 新增可靠的 `data.yml` 持久化与原子文件替换。
- 新增多单位时间解析并修复药水持续时间溢出。
- 修复空签池先扣款、异常不退款、重复点击、无效权重与背包溢出问题。
- 统一 MiniMessage 文本入口并兼容旧版 `&` 颜色代码。
- 配置升级改为只补全缺失节点，不覆盖自定义值。
- 构建脚本改用 Java 25 和 Paper 26.1.2 精确依赖白名单。
